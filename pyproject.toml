[project]
name = "carrito"
version = "0.1.0"
description = "Cálculo del total de un pedido."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/carrito"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
